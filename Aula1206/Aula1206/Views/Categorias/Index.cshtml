@{
    ViewBag.Title = "Categorias";
}

<h2>@ViewBag.Title</h2>

@foreach (string cat in ViewBag.ListaCategorias)
{
    //<p>@cat</p>
    //<a href="#">@cat</a>
    @Html.ActionLink(@cat, "Index", "Categoiras")
    @: |
}

